node_modules/
dist/
.cache/
*.log
.DS_Store
*.local
coverage/
tools-opsin-*.jar
tools/audit-output/

# Checkout de ferramentas de agente: repositório próprio, não faz parte do app.
# Estava commitado como gitlink sem .gitmodules, o que quebrava todo clone novo
# (e com ele o build do Netlify).
.agents/
